apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: araba-degerleme
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth-service
  template:
    metadata:
      labels:
        app: auth-service
    spec:
      containers:
        - name: auth-service
          image: araba-degerleme/auth-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8001
---
apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: araba-degerleme
spec:
  selector:
    app: auth-service
  ports:
    - port: 8001
      targetPort: 8001
